{
  "name": "colormath-contact-form",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that emails /contact form submissions via Resend.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260923.1",
    "@types/node": "^22.20.4",
    "typescript": "^5.9.3",
    "wrangler": "^4.137.0"
  }
}
